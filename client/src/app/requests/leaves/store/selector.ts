// import { state } from "@angular/animations";
// import { createSelector } from "@ngrx/store";
// import { AppInterfaceState } from "src/app/domain/app.interface";

// export const selectorFeature = (state:AppInterfaceState) => state.leaves;
// export const leaveSelector = createSelector(selectorFeature,(state) => state.Leaves);